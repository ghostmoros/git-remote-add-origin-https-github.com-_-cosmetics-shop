# Профессор 🎓

Автоматический бот для заработка по двум направлениям:

1. 📈 **Крипто-трейдинг** — стратегия + бэктест на свечах (paper trading).
2. 🎲 **Polymarket** — сканер возможностей на prediction-маркете
   (value-ставки и арбитраж).

По умолчанию всё работает в режиме **симуляции**: без реальных денег, без
ключей и **без обязательного доступа в интернет**. Чистый Python (только
стандартная библиотека) — запускается где угодно, `pip install` не нужен.

> ⚠️ **Не финансовый совет.** Трейдинг и ставки рискованны. Всё по умолчанию
> крутится на синтетических данных. Перед реальными деньгами проверяйте на
> демо и осознавайте, что можно потерять весь капитал.

## Быстрый старт

```bash
python3 main.py          # симуляция (по умолчанию)
python3 main.py --live   # попытаться взять реальные публичные API,
                         # при блокировке — автооткат на симуляцию
python3 main.py --wf     # walk-forward анализ Donchian breakout
                         # (честная out-of-sample проверка vs переобучение)
```

Пример вывода:

```
==================================================================
  ПРОФЕССОР v0.1.0  —  крипто-трейдинг + Polymarket
==================================================================
Режим: СИМУЛЯЦИЯ (paper, без риска)  ·  капитал на символ: $1,000

📈 КРИПТО-ТРЕЙДИНГ (симуляция, paper trading)
Символ      Сделок   Винрейт      PF      Доход   Просадка
...

🎲 POLYMARKET — НАЙДЕННЫЕ ВОЗМОЖНОСТИ
Тип         Сторона   Цена  Честн.    Edge    EV/$  Kelly  Вопрос
...
```

## Реальные данные (CSV / live)

В некоторых окружениях прямой доступ к биржам закрыт сетевой политикой (API
отдают 403) — тогда котировки не качаются «на лету». Два способа работать на
настоящих данных:

**1. CSV-файл (работает где угодно, без сети):**

```bash
python3 main.py --csv data/BTCUSDT-1d.csv        # обычный прогон
python3 main.py --wf --csv data/BTCUSDT-1d.csv   # walk-forward на реальных данных
```

Где взять бесплатные исторические OHLCV:
- **Binance public data** — https://data.binance.vision (просто файлы, без ключей,
  напр. `spot/monthly/klines/BTCUSDT/1d/`)
- **CryptoDataDownload** — https://www.cryptodatadownload.com
- **Kaggle** — крипто-датасеты

Загрузчик понимает формат с заголовком (`open,high,low,close,volume[,time/date]`)
и без заголовка (стиль Binance: `time,open,high,low,close,volume,...`) и сам
сортирует по времени.

**2. Live-режим (нужна сеть к биржам):**

```bash
python3 main.py --live
```

Заработает в окружении, чья сетевая политика разрешает биржевые домены. Подробнее
про сетевые политики — https://code.claude.com/docs/en/claude-code-on-the-web

## Как считается прибыльность

Высокий **винрейт** ≠ прибыль. Решает **матожидание (expectancy)**:

```
expectancy = win_rate × средний_выигрыш − loss_rate × средний_проигрыш
```

Отчёт всегда печатает винрейт, profit factor и доходность вместе — чтобы
оптимизировать правильную метрику, а не просто «процент побед».

## Polymarket: что ищет сканер

На prediction-маркете доля **YES/NO** стоит от 0 до 1 и выплачивает $1 при
выигрыше. Цена ≈ вероятность исхода по мнению рынка. «Профессор» ищет:

- **Арбитраж** — когда `цена(YES) + цена(NO) < 1`: покупаешь обе стороны и
  гарантированно забираешь разницу при любом исходе.
- **Value-ставки** — когда наша «честная» оценка вероятности заметно выше
  рыночной цены (есть **edge**). Для каждой возможности считается ожидаемая
  прибыль на $1 и рекомендуемая доля банка по **Келли** (дробный, ¼).

> В симуляции «честная» вероятность генерируется (часть рынков намеренно
> недооценена). В реальном режиме сюда подключается ваша модель/оценка.

## Структура

```
professor/
  config.py              # все настройки
  trading/
    data.py              # свечи: синтетика + (опц.) Binance
    indicators.py        # SMA / EMA / RSI
    strategy.py          # SMA-cross + RSI-фильтр; Donchian breakout
    backtest.py          # paper-брокер + прогон (long-only, с прогревом)
    walkforward.py       # walk-forward: честная out-of-sample проверка
    metrics.py           # винрейт, profit factor, expectancy, просадка
  polymarket/
    models.py            # модель рынка
    client.py            # рынки: синтетика + (опц.) Gamma API
    scanner.py           # поиск арбитража и value-ставок
  report.py              # отчёты в консоль
  engine.py              # связывает всё вместе
main.py                  # точка входа
tests/                   # юнит-тесты (stdlib unittest)
```

## Тесты

```bash
python3 -m unittest discover -s tests
```

## Дорожная карта

- [ ] Больше стратегий (mean-reversion, breakout) и их сравнение
- [ ] Реальные данные в live-режиме (когда есть доступ к API/ключам)
- [ ] Своя модель оценки вероятностей для Polymarket
- [ ] Управление капиталом между двумя направлениями
- [ ] Логи сделок и сохранение истории прогонов
