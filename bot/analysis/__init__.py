"""Analysis tools that sit outside the strategy/backtest loop.

Currently: `otc` — test whether a broker's synthetic ("OTC") price feed is
distinguishable from random and whether any pattern survives out-of-sample.
"""
